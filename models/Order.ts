export interface Order {
  orderId: string;
  productId: string;
  currency: string;
  quantity: string;
  shippingCost: string;
  amount: string;
  channel: string;
  channelGroup: string;
  campaign: string;
  dateTime: string;
}
